public interface ConteudoPaginacao {
    /**
     * @param p recebe um produto P.
     * @return retorna uma string formatada com a listagem individual daquele produto.
     */
    String stringPorListagem(Produto p);
}
